/** @type {import('next').NextConfig} */
const nextConfig = {
  // We don't need experimental.appDir in Next 14.
  // Leave this object empty unless we add other options later.
};

export default nextConfig;
