"use client";

export default function ThemeInitScript() {
  // Minimal: keeps your existing theme wiring from exploding.
  // If you already store theme elsewhere, we can align later.
  return null;
}
